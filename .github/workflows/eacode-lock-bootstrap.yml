name: EACODE - Production Lock Bootstrap

on:
  push:
    branches:
      - EACODE
    paths:
      - estimador-cag/deploy/eacode/pyproject.toml
      - .github/workflows/eacode-lock-bootstrap.yml

permissions:
  contents: write

jobs:
  generate-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact bootstrap head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Verify exact checked-out SHA
        run: test "$(git rev-parse HEAD)" = "${{ github.sha }}"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve isolated EACODE production lock
        run: uv lock --project estimador-cag/deploy/eacode

      - name: Record lock digest
        run: |
          cd estimador-cag/deploy/eacode
          sha256sum uv.lock > uv.lock.sha256

      - name: Commit deterministic lock to canonical branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add estimador-cag/deploy/eacode/uv.lock estimador-cag/deploy/eacode/uv.lock.sha256
          git diff --cached --check
          git commit -m "eacode: lock isolated production dependencies"
          git push origin HEAD:EACODE
