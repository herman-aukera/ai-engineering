"""
LAYER: middleware (package root)
RESPONSIBILITY: Marks the middleware sub-package.
WHY IT EXISTS: Enables clean imports like `from app.middleware.logging import setup_logging`.
DEPENDS ON: Nothing (package root)
"""
# Middleware package
